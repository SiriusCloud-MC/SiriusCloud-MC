* text=auto eol=lf

# Windows launchers must keep CRLF or cmd.exe mis-parses them.
*.bat   text eol=crlf
*.cmd   text eol=crlf

# POSIX launchers must keep LF even when checked out on Windows.
gradlew text eol=lf
*.sh    text eol=lf

*.jar   binary
*.png   binary
